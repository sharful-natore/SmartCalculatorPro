package com.example.ui.viewmodel

/**
 * MainViewModel is a typealias for CalculatorViewModel, which serves as the central
 * ViewModel managing activeTab, theme, history, tools, and app-wide state.
 */
typealias MainViewModel = CalculatorViewModel
